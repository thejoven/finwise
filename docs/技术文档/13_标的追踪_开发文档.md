# 13 · 标的追踪 · 开发文档

> 作者视角:**数据 / 行情工程师 + 产品**。
> 这份文档回答:信号被发现后,如何把它指向的**标的**钉在"发现时刻",并让你点进去看见**发现之后这只标的的股价怎么走**——给归档复盘、承诺持仓一面客观的照妖镜。
> 它是**规格**(做什么 / 为什么 / 三个硬问题 / 数据模型 / 分期),尚未进入执行单。

- 状态: **P0+P1 已落地 2026-06-16;P2 承诺表现后端 + P4 标的专页反查端点 2026-06-17** (见 §6 + §6.1–§6.6 落地纪要); 新信号实时归一已接; **标的追踪页 Hub 聚合端点 `GET /v1/track/overview` 已落地 2026-06-17**(新增需求 §6.6); 待做: 移动端 UI(Hub 页 + P2 曲线/P3 sparkline/P4 专页)+ 把收益喂 [12]
- 日期: 2026-06-13 (规格) · 2026-06-16 (P0 落地)
- 前置: 信号已带 `related_assets`([signal.go](../../server/internal/domain/signal.go) + [beneficiary agent](../../mastra/src/agents/beneficiary.ts));承诺已带 `thesis.asset_ticker`。**全栈尚无任何行情数据接入**
- 姊妹文档: [12 主动信号推荐](12_主动信号推荐_开发文档.md) —— 本文产出的"发现后收益"是它用户画像里唯一的客观地面真值,P4 起回灌

---

## § 0 · 一句话与它为什么重要

**一句话**:给每个信号 / 承诺的标的,以"你发现它的那一刻"为锚点,缓存并展示其后的日线走势与累计涨跌,点击标的进入它的专页,看见你所有碰过它的命题 + 这条价格路径。

**为什么重要——它补上产品缺的那一环**:
现在的产品帮你**想**(捕捉→精炼→四道门→承诺→复盘),却从不告诉你**想对了没有**。复盘环节问的是"你当时怎么想",却没有"后来怎么样"。标的追踪是这条闭环上缺失的**结果层 / 地面真值**:

- 让**复盘诚实**:你放下的、你押下的,事后价格给出客观回答。
- 给 **[12 推荐](12_主动信号推荐_开发文档.md)** 喂真值:画像才能从"像你过去"进化到"像你看得准"。
- 给用户**体感**:一条绿/红的曲线,比任何文案都更能校准判断力。

不做投资建议、不报实时 tick、不出买卖提示——只呈现"发现后,它怎么走了"。与"不替用户下单 / 移钱"的既定立场一致。

---

## § 1 · 这份文档要回答的核心问题

1. 标的怎么从一堆自由文本(`related_assets`)归一成行情源认得的**规范代码**?(§3 硬问题一)
2. 行情数据从**哪来**——A股/港股/美股,且要能翻过 GFW 在 205 上跑通?(§3 硬问题二)
3. "发现后"的**锚点**到底取哪个时间?(§3 硬问题三)
4. 加哪几张表存标的与价格?(§4)
5. 后端 / Mastra / 移动端各加什么?哪些界面长出曲线?(§5)
6. 目前阶段先做哪一刀、怎么分期?(§6)
7. 数据诚实、口径、失败模式怎么处理?(§7)

---

## § 2 · 现状盘点

| 已有 | 在哪 |
|---|---|
| 信号的派生标的 `related_assets: [{ticker, rationale, order}]` | [`signals`](../../server/migrations/002_create_signals_outbox.up.sql) + [beneficiary agent](../../mastra/src/agents/beneficiary.ts) |
| 承诺标的 `thesis.{asset_ticker, asset_name}` | [`commitments`](../../server/migrations/004_create_gates_commitments.up.sql) |
| 降噪页已按标的展示受益方 | [`DenoiseView`](../../mobile/src/features/capture/DenoiseView.tsx) |
| 统计页已用图表库画曲线(可复用) | [`AttentionScreen`](../../mobile/src/features/attention/AttentionScreen.tsx) |
| **缺**:规范代码 / 行情源 / 价格缓存 / 锚点 / 任何价格 UI | —— |

`related_assets[].ticker` 目前是 **LLM 自由产出**(可能是"宁德时代""300750""NVDA""0700.HK"混杂),不能直接喂行情源——这正是 §3 硬问题一。

---

## § 3 · 三个硬问题(诚实面对)

### 硬问题一:标的归一(symbol resolution)

beneficiary agent 现在吐的是人话/混杂代码。要追价格,必须先归一到 **{规范代码, 交易所, 市场}**。

**方案**:

1. **升级 beneficiary**(及 analyst)产出**结构化**标的:`{name, symbol, exchange, market}` 而非裸 ticker;旧数据走一次性 resolver 回填。
2. **Go 侧 resolver + `assets` 注册表 + 别名表**:LLM 归一后,在 `assets` 里 upsert 规范记录;命中别名(`asset_aliases`)直接复用,省 LLM。
3. **人工兜底端点**:LLM 必然有归一不了的(同名、新股、非上市受益方)。提供手动指定 / 标记"不可追踪",**诚实留空好过追错**(呼应 [信号永不未分类](../../server/internal/module/signal) 的"宁可诚实兜底也不给错值")。

### 硬问题二:行情源(关键决策)

需要 A股/港股/美股的**日线 OHLC**(实时/分钟线非必须)。约束:产品中文向、部署在 **205 且在 GFW 后**。

| 选项 | 覆盖 | 反 GFW / 可达 | 代价 |
|---|---|---|---|
| Tushare Pro | A股强、港股可、美股弱 | 国内可达 ✓ | 需 token、有频控/积分 |
| AkShare | A/港/美都广 | 国内可达 ✓ | Python 库,需起 sidecar 进程 |
| 东方财富/新浪 非官方 | A/港/美(分端点) | 国内可达 ✓ | 免费但脆、无 SLA、易变 |
| Alpha Vantage / Finnhub / Polygon | 美股强、A股弱 | GFW 后可达性存疑 | 美股为主,A股不行 |

**建议**:做**行情源抽象**(interface + adapter,照 [LLM model 工厂](../../mastra/src/llm/model.ts) 与 [twtapi client](../../server/internal/infra/twtapi/client.go) 的可换源风格),把"选哪家"与"怎么用"解耦。默认源**待 205 实测可达性后定**(见 §8 决策一)。先用一家把 A股跑通,港美随抽象扩。

### 硬问题三:锚点("发现后"从哪天起)

"发现后"必须有明确、**冻结**的起点,否则收益数字可被事后挑拣,失去诚实性。

- **锚点 = 信号 `captured_at`**(你/系统登记它的时刻)为规范"你发现的时刻"。
- 对**转信号的推文**,底层 `tweets.tweet_created_at` 是"信号真正出现"的更早时刻——作为**次要标记**叠加展示("原文发布于…"),但收益锚点仍用 `captured_at`(那是**你**发现的时刻,不是别人发推的时刻)。
- 锚点在归一时**冻结**写入 `signal_assets.anchor_at`,此后不随重算变动。
- 收益 = `(最新收盘 − 锚点日收盘) / 锚点日收盘`。

---

## § 4 · 数据模型

派生/缓存数据,**不写 events**(同既有先例)。

**`assets`** —— 规范标的注册表

```sql
CREATE TABLE assets (
    id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    canonical     text NOT NULL,        -- 规范代码,如 "300750" / "00700" / "NVDA"
    exchange      text NOT NULL,        -- "SZSE" / "SSE" / "HKEX" / "NASDAQ" …
    market        text NOT NULL CHECK (market IN ('a','hk','us')),
    name          text NOT NULL,
    provider_symbol text,               -- 行情源各自的代码(如 "300750.SZ")
    type          text NOT NULL DEFAULT 'equity',
    status        text NOT NULL DEFAULT 'active',  -- active|delisted|untrackable
    created_at    timestamptz NOT NULL DEFAULT now(),
    UNIQUE (market, canonical)
);
```

**`asset_aliases`** —— LLM/用户给的各种叫法 → asset_id(省重复归一)

```sql
CREATE TABLE asset_aliases (
    alias_lower text PRIMARY KEY,       -- 归一化后的别名("宁德时代" / "ningde" / "catl")
    asset_id    uuid NOT NULL REFERENCES assets(id)
);
```

**`asset_prices`** —— 日线缓存

```sql
CREATE TABLE asset_prices (
    asset_id  uuid NOT NULL REFERENCES assets(id),
    date      date NOT NULL,
    open      numeric, high numeric, low numeric, close numeric NOT NULL,
    volume    bigint,
    source    text NOT NULL,
    PRIMARY KEY (asset_id, date)
);
```

**`signal_assets`** —— 信号 ↔ 标的链接 + 冻结锚点(**取代**只靠 JSON 查)

```sql
CREATE TABLE signal_assets (
    signal_id  uuid NOT NULL REFERENCES signals(id),
    asset_id   uuid NOT NULL REFERENCES assets(id),
    role       text NOT NULL DEFAULT 'beneficiary',  -- beneficiary | primary | mentioned
    anchor_at  timestamptz NOT NULL,                  -- 冻结 = 解析时的 signal.captured_at
    rationale  text,
    PRIMARY KEY (signal_id, asset_id)
);
```

> **为什么用链接表而非继续塞 JSON**:它一表服务三个查询——
> ① 本文的"某信号/承诺的标的走势";
> ② [12 推荐](12_主动信号推荐_开发文档.md) 的"关于标的 X 的新情报";
> ③ 反查"标的 X 涨了 → 我哪些命题碰过它"(标的专页的核心)。
> JSON 做不到第②③。承诺侧同理可加 `commitment_assets`,或承诺直接复用其 evaluation→refinement→signal 链路反查。

---

## § 5 · 各层改动

**Go**:

- `internal/infra/marketdata/` —— 行情源抽象 + 首个 adapter(interface、`IsConfigured()`、typed errors、env 配置,照 [twtapi](../../server/internal/infra/twtapi)/[mastra client](../../server/internal/infra/mastra/client.go))。
- `internal/module/asset/`:
  - `resolver.go` —— 归一(别名命中优先,未命中调 Mastra/规则)→ upsert `assets`/`asset_aliases`/`signal_assets`(冻结锚点)。
  - `poller.go` —— 价格回填 + 日更 worker(照 [subscription poller](../../server/internal/module/subscription/poller.go):新标的从 `anchor_at` 回填;每日 tick 给所有被追踪标的补最新 bar;`SKIP LOCKED` 取批;配额/退避处理频控)。
  - `handler.go` —— 读端点:
    - `GET /v1/assets/:id/prices?from=&to=` —— 原始日线
    - `GET /v1/signals/:id/track` —— 解析该信号标的,返回 `[{asset, anchor_at, anchor_close, latest_close, pct_change, series[]}]`
    - `GET /v1/commitments/:id/track` —— 承诺标的,锚点可叠加签字日(最高价值入口)
    - `GET /v1/assets/:id/theses` —— 反查"我碰过它的所有命题"(标的专页)
    - `POST /v1/assets/resolve`(内部/人工兜底)—— 手动指定 / 标记不可追踪

**Mastra**:

- 升级 [beneficiary.ts](../../mastra/src/agents/beneficiary.ts) 产出结构化 `{name, symbol, exchange, market}`(向后兼容旧裸 ticker)。
- 可选 `symbol-resolve` 端点供 resolver 调用做歧义归一(同六端点同形)。

**Mobile**(`src/features/track/`,图表复用 [attention 的图表库](../../mobile/src/features/attention/AttentionScreen.tsx)):

- **可复用价格组件**:折线(收盘),锚点处一条"发现"竖线标记,顶部醒目累计涨跌(涨跌色克制、遵循 [深色模式 token](../../mobile/src/shared)),承诺页叠加"签字日"标记。
- 落点:
  - **标的追踪页 (Hub · 着陆页, 新增需求 2026-06-17)** —— 罗列最新有关的三段: 关联标的(你碰过的标的 + 最新价 + 发现至今 %)、信号(最近带标的的信号 + 其标的)、订阅信息(最新相关推文)。各项点进对应 detail。后端 `GET /v1/track/overview` 一次取齐。
  - **信号详情** `/signal/[id]` —— 每个标的一条 sparkline + "发现至今 %"。
  - **降噪行** —— 受益标的旁一枚微型 sparkline + 涨跌角标(一眼看出"这条读对没")。
  - **承诺/持仓** `/commitment/[id]` —— 锚定签字日的完整曲线,"你这笔承诺的表现"(情绪兑现点)。
  - **归档/复盘** —— 被否信号标的的价格路径:"你当时放下的,后来怎么样了"——校准四道门是否否对了。
  - **标的专页**(点击任意标的):完整曲线 + `GET /assets/:id/theses` 列出你碰过它的每条信号/承诺。**这正是用户原话"点击后查看对应发现后的股价变动"。**
- API 客户端 `core/api/track.ts`(照 [gate.ts](../../mobile/src/core/api/gate.ts))。

---

## § 6 · 分期 —— "目前阶段先做哪一刀"

| 期 | 名称 | 范围 | 为什么这个顺序 | DoD |
|---|---|---|---|---|
| **P0** ✅ | 标的归一 (**已落地 2026-06-16**) | `assets`/`aliases`/`signal_assets`(迁移 **025**)+ resolver(别名→规则→Mastra `/symbol-resolve`→诚实兜底);beneficiary 结构化(market/exchange 可选, 向后兼容);`cmd/asset-backfill` 回填存量 | 一切的前提;无价格也能先验证"归一对不对" | ✅ 40 信号/164 引用回填: **45 归一**(A17/HK6/US22, 抽查全对)、**71 诚实标 untrackable**(加密/未上市/海外/篮子)、0 错误;`POST /v1/assets/resolve` 人工兜底(集成测试通过) |
| **P1** ✅ | 行情底座 (**已落地 2026-06-16**) | `marketdata` 抽象(Provider)+ tencent/eastmoney 两 adapter(默认 tencent)+ `asset_prices`(迁移 **029**)+ 回填/日更 poller + `cmd/asset-price-sync` + `GET /assets/:id/prices` | 205 实测定源(东方财富被封→改腾讯),A股日线跑通 | ✅ 16 只真实 A股从锚点回填到今日(446 bar)、日更幂等(重跑 0 新行)、`GET prices` 读路径集成测试过 |
| **P2** 🟡 | 承诺表现(最高价值)(**后端已落地 2026-06-17**) | ✅ `GET /v1/commitments/:id/track`(+ `/v1/signals/:id/track`)复用链路反查;⬜ 承诺/持仓页曲线 UI(移动端) | "我这笔押对没"是情绪兑现点,直接 | ✅ 后端: 2 签字承诺均返锚定曲线 + 发现/签字双锚累计涨跌(untrackable 篮子诚实留空);⬜ 移动端承诺页曲线待做 |
| **P3** | 信号/降噪 sparkline | 信号详情 + 降噪行的"发现至今 %"微图 | 把结果带到日常浏览,纯增量 | 降噪行一眼见涨跌 |
| **P4** 🟡 | 标的专页 + 归档联动 + 喂给推荐 | ✅ `GET /v1/assets/:id/theses`(全命题反查);⬜ 专页/归档 UI、`signal_assets` 收益暴露给 [12](12_主动信号推荐_开发文档.md) | 完成用户原话的"点击查看";闭合到推荐真值 | 🟡 反查端点已落地(实测鼎龙股份返 2 条签字承诺);专页 UI + 喂 12 待做 |

**当前阶段建议**:**P0 → P1 → P2**。P0/P1 是地基(没有归一和行情,后面全空中楼阁);P2 是用户最先想看见的"我押对没"。P4 与 [12](12_主动信号推荐_开发文档.md) 排程对齐。

### § 6.1 · P0 落地纪要 (2026-06-16)

worktree `feat/asset-tracking-p0`,205 验证通过 (`go build/test` 全绿、mastra `typecheck` 过、迁移 up/down 可逆、回填抽查)。

- **迁移 025** `025_asset_tracking`(**非原拟 023** —— 023 被 `user_language`、024 被姊妹 [12](12_主动信号推荐_开发文档.md) 占用,顺延 025;三号互不冲突)。三表派生数据,**不写 events**。
- **DDL 较 §4 草案两处务实演进**(草案是规格,落地需自洽): `market` CHECK 增 `'other'`(承载 untrackable —— a\|hk\|us 无处安放归一不了的行);`exchange NOT NULL DEFAULT ''`(美股一时判不准 NASDAQ/NYSE,宁可留空不谎报);`status ∈ active\|delisted\|untrackable`。
- **resolver** (`server/internal/module/asset/`): 别名缓存命中 → 规则(纯数字 A/HK 代码、括号内嵌代码,确定性不调 LLM)→ Mastra `/symbol-resolve`(中文名/裸字母 ticker,LLM 归一)→ **诚实兜底 untrackable**。LLM 输出经**结构校验**(代码格式须与 market 匹配)兜格式型幻觉。
- **Mastra**: 新 `symbol-resolver` agent(高确信才给码,加密/未上市/海外/篮子/歧义一律 `resolvable:false`)+ `POST /symbol-resolve`;`beneficiary` 加 `market`/`exchange` 可选字段(向后兼容旧裸 ticker)。
- **人工兜底** `POST /v1/assets/resolve`(手动指定 canonical/exchange/market,或标 untrackable;可选链信号、冻结锚点)。
- **存量回填** (`cmd/asset-backfill`,幂等可重跑): 40 信号 / 164 引用 → **45 归一**(A股 17 / 港股 6 / 美股 22,人工抽查代码与交易所全对)、**71 诚实标 untrackable**、**0 错误**。`signal_assets` 163 链接 / 40 信号,锚点 100% 冻结 = `captured_at`。

**P0 不含**(留 P1+): `asset_prices` 行情缓存、marketdata 抽象 + 行情源、poller、价格/曲线 UI;新信号的**实时**归一(把 resolver 接进 signal-inference 链路)亦留后续 —— 现仅 backfill + 人工兜底。

**已知遗留**: ① 个别引用(如 `COIN`)配 rationale 会触发 DeepSeek structured-output 偶发失败,已加 context-free 重试兜住(回填 0 错误); ② 源数据若给错括号代码(如 "安集科技 (688128)"),resolver 忠实采用括号内代码 → 名实可能不符,靠人工兜底纠。

### § 6.2 · P1 落地纪要 (2026-06-16)

205 验证通过 (`go build/test` 全绿、迁移 029 up/down 可逆、A股回填抽查、日更幂等)。

- **迁移 029** `029_asset_prices`(025↔029 间 026 推荐/027 recovery/028 research_dedup 被姊妹/他务占,顺延 029): `asset_prices` 日线缓存(PK `(asset_id,date)`)+ `assets` 加价格轮询状态列(`price_status/checked_at/synced_at/attempts`)。
- **行情源抽象** `internal/infra/marketdata`(`Provider` 接口: `Supports(market)` + `DailyBars(market,canonical,from,to)`,typed errors,工厂 `New(name)`),换源只改 adapter + 一个 env。
- **默认源 = 腾讯 ifzq**(免费/国内可达/前复权/支持日期段)。**原定东方财富,但 205 实测它连续请求后封了本机 IP**(非官方源脆弱,§3 表已警示)→ 改腾讯;两 adapter 都在,`MARKETDATA_PROVIDER` 可切。poller 加 400ms 请求间隔 + 轻量重试,别再把 IP 打封。
- **poller** (`asset/poller.go`,形态同 subscription poller): 认领 `status=active` 且 `price_status∈(pending,active)` 标的(`SKIP LOCKED`),pending 从 `min(anchor_at)` 回填到今日、active 从最新 bar 日更(`ON CONFLICT DO UPDATE` 吸收前复权漂移);hk/us 标 `unsupported`(P1 无其 adapter)、限流全局退避、连失 5 次熔断 `failed`。`cmd/asset-price-sync` 是"立即跑一遍"入口。
- **读端点** `GET /v1/assets/:id/prices?from=&to=`: 原始日线 + 行情出处 + 数据 as-of。
- **回填结果**: 16 只真实 A股从各自锚点回填到今日共 **446 bar**,`price_status` 全 active,`first_bar` 与冻结锚点严丝合缝、`last_bar`=今日;"发现至今 %" 可算(如中芯国际 −15.3% / 鼎龙股份 +6.2%)。日更幂等已验(重跑 17 标的、0 新行)。

**P1 不含**(留 P2+): 港/美 adapter(腾讯也能做,留市场范围拍板后接)、把 resolver 接进 signal-inference 做新信号实时归一、价格/曲线 UI、承诺侧 `/commitments/:id/track`。

### § 6.3 · P2 落地纪要 (2026-06-17, 后端)

承诺/信号"发现后走势"读端点(移动端曲线 UI 待做)。205 `go build/test ./...` 绿 + 线上 smoke。

- **`GET /v1/commitments/:id/track`** + **`GET /v1/signals/:id/track`**(asset 模块 handler): 承诺侧叠加签字日锚点。
- **chain**(只读跨表 JOIN,**未新建 `commitment_assets` 表**,§8 决策三): commitments → gate_evaluations → refinement_sessions.primary_signal_id → signal_assets → assets → asset_prices。
- **buildTrack**: series 裁到发现日起;`anchor_close` = 发现后首个交易日收盘;承诺侧 `sign_close` = 签字后首个交易日收盘;输出 `pct_since_discovery` / `pct_since_sign`。untrackable / 无价 → 空 bars + nil close(诚实留空)。
- **线上 smoke**: 2 签字承诺(CMP slurry / CMP-SLURRY)均返曲线 —— 鼎龙股份 +6.1%/+15.0%、长电科技 −12.8%、华海清科 −4.4% 等;篮子 "SK Hynix / Samsung"、"长电/通富/华天" 正确空 bars。信号 track 同样可用(无签字锚)。

**P2 不含**(留后续): 移动端承诺/持仓页曲线组件(签字日竖线标记 + 顶部累计涨跌)。

### § 6.4 · P4 标的专页反查 (2026-06-17, 后端)

**`GET /v1/assets/:id/theses`** —— 反查某 user 碰过该标的的全部命题: `signal_assets[asset_id]` → signals(user-scoped)LEFT JOIN refinement_sessions→gate_evaluations→commitments。返回标的 meta + theses[](锚点 / role / rationale / signal summary + 派生承诺 status/signed_at/action;`kind=signal|commitment`)。用 `idx_signal_assets_asset` 反查;标的不存在 → 404,user 没碰过 → 空。实测鼎龙股份 300054 返 2 条签字 buy 承诺。**P4 其余(专页/归档移动端 UI、把 `signal_assets` 收益喂给 [12](12_主动信号推荐_开发文档.md) 作画像真值)留后续。**

### § 6.5 · 新信号实时归一 (2026-06-17)

把 resolver 接进推演回写,闭环自持: `signal.RecordInference` 落库后经 `AfterInferenceFn` hook(main.go 装配的异步闭包)调 `asset.ResolveSignal(signalID)` —— **新信号一推演完,其 `related_assets` 即自动归一成 `signal_assets`(冻结锚点=captured_at),随后 price poller 自动给新 trackable 标的回填行情**。best-effort(异步 + 吞错,不阻塞推演),兜底仍是 `cmd/asset-backfill` + `POST /v1/assets/resolve`。`signal.Service` 用 `SetAfterInference` setter 接(不改构造签名 / 不破测试),闭包形式避免 signal 反向 import asset。实测: 删某信号 signal_assets → 重投推演 → hook 自动重建 5 条链接。**至此归一不再只靠一次性 backfill / 人工**。当前 43/43 有标的的信号全部已链。

### § 6.6 · 标的追踪页 Hub (新增需求 2026-06-17)

**需求**: 标的追踪要有一个着陆/聚合页, **罗列所有最新有关的**: ① 关联标的(用户碰过的标的, 最新价 + 发现至今涨跌) ② 信号(最近带标的的信号) ③ 订阅信息(最新相关推文)。原 §5 的各 detail 落点(信号/承诺/专页)是从这个 Hub 点进去的下钻。

**后端**(已落地 2026-06-17): `GET /v1/track/overview?limit=` —— 一次取齐三段(user-scoped):
- `assets`: 用户碰过的标的(`signal_assets⋈signals` 去重),按 last_touched DESC;每项 = 标的 meta + status + price_status + latest_close/date + anchor_close(最早锚点)+ pct_since(发现至今)+ thesis_count。
- `signals`: 最近带标的的信号(captured_at DESC)+ 各自归一后的标的。
- `tweets`: 最新订阅推文(复用 `subscription.Feed`;带 summary/category/tags/relevance),"有关"由 relevance 体现。

跨表只读聚合(assets/signals 走 asset repo;tweets 经注入闭包复用订阅模块,不反向 import)。**移动端 Hub 页 UI 待做**(见 §5)。

---

## § 7 · 数据诚实、口径、失败模式

- **诚实兜底**:归一不了 → `status='untrackable'`,UI 显示"无法追踪此标的",**绝不**用模糊匹配凑一个可能错的代码(错的价格比没价格更有害)。
- **锚点冻结**:`anchor_at` 一经写入不随重算变动,杜绝事后挑拣起点。
- **数据出处**:UI 标注行情来源 + 数据 as-of 日期;停牌/退市显式处理(`assets.status='delisted'`,曲线断点)。
- **不是投资建议**:无实时 tick、无买卖提示、不替用户下单/移钱(既定立场);仅"发现后它怎么走"。
- **失败模式**:行情源不可用 → 已缓存照常展示 + 标"数据截至 X";新标的回填失败 → 重试 + 标"暂无数据",不阻塞信号主流程。
- **成本/频控**:全局标的去重(多个信号同一标的只缓存一份,同订阅 `tweets` 全局共享精神);poller 退避处理源频控。

---

## § 8 · 开放决策(执行前需拍板)

1. ~~**默认行情源**~~ —— **已定 (2026-06-16, P1)**: 默认 **腾讯 ifzq**(免费/国内可达/前复权/日期段查询)。原拟东方财富,但 205 实测它连续请求后封本机 IP → 改腾讯;经 `marketdata.Provider` 抽象,`MARKETDATA_PROVIDER` 可切,将来可上 Tushare 付费源求 SLA。
2. **市场范围** —— P1 已按 **A股首发** 落地(腾讯 adapter 仅 `Supports("a")`);抽象层已就绪,加 hk/us adapter 即可扩,**但首发只 A股还是 A+港+美仍待产品拍板**(影响 beneficiary 归一与回填工作量)。
3. ~~**承诺标的链接**~~ —— **已定 (2026-06-17, P2)**: 复用 `commitments→gate_evaluations→refinement_sessions→signal_assets` 链路反查(未新建 `commitment_assets` 表),`GET /v1/commitments/:id/track` 已落地。
4. **日线 vs. 分钟线** —— "发现后变动"日线足够;分钟线留作后续可选,不进首发。
